model Article {
  id                    BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  publisher             Publisher              @relation(fields: [publisherId], references: [id], map: "fk_article_publisher_id", onDelete: Cascade, onUpdate: Cascade)
  publisherId           BigInt                 @map("publisher_id") @db.UnsignedBigInt
  title                 String                 @db.VarChar(255)
  link                  String                 @unique(map: "uq_article_link") @db.VarChar(255)
  author                String                 @db.VarChar(255)
  publishedAt           DateTime               @map("published_at") @db.DateTime()
  articleLabelRelations ArticleLabelRelation[]
  createdAt             DateTime               @default(now()) @map("created_at") @db.DateTime()
  updatedAt             DateTime               @default(now()) @updatedAt @map("updated_at") @db.DateTime()

  @@map("article")
}
