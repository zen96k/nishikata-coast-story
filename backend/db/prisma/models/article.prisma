model Article {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  publisher  Publisher
  title      String
  author     String
  published  DateTime
  updated    DateTime
  created_at DateTime
  updated_at DateTime
}

enum Publisher {
  Qiita
  Zenn
}
