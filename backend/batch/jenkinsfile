pipeline {
    agent any

    tools {
        nodejs "NodeJS-22"
    }

    stages {
        stage('test') {
            steps {
                sh "cd backend/batch && pwd && npm run ncs:test"
            }
        }

    }
}
