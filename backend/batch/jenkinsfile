pipeline {
    agent any

    tools {
        nodejs "NodeJS-22"
    }

    stages {
        stage('test') {
            steps {
                sh "npm run ncs:test"
            }
        }

    }
}
