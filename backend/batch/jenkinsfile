pipeline {
    agent any

    tools {
        nodejs "node:22"
    }

    stages {
        stage('test') {
            steps {
                sh "cd backend/batch && npm clean-install && UID=1000 GID=1000 npm run ncs:test"
            }
        }
    }
}
