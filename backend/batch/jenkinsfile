pipeline {
    agent any

    tools {
        nodejs "node:22"
    }

    stages {
        stage('test') {
            steps {
                sh "cd backend/batch && npm clean-install && npm run ncs:test"
            }
        }
    }
}
